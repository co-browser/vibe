# Gmail MCP Server Configuration

# Server port (default: 3001)
PORT=3001

# Google OAuth Configuration (Required for token refresh in cloud mode)
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# Local vs Cloud Mode
# USE_LOCAL_GMAIL_SERVER=false  # Set to true for local mode with file-based auth

# Path to OAuth credentials (if using local file-based auth)
# GMAIL_OAUTH_PATH=/path/to/gcp-oauth.keys.json
# GMAIL_CREDENTIALS_PATH=/path/to/credentials.json 