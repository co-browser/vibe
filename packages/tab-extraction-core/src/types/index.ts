// Import and re-export from shared types
import { PageContent, ExtractedPage, PageMetadata } from "@vibe/shared-types";
export { PageContent, ExtractedPage, PageMetadata };

// Re-export from shared types
export { CDPTarget, TabInfo } from "@vibe/shared-types";
